<div class="container">

<h1>Burger Time</h1>
<br>

<h2>Type your dream burger in the box.  Then devour it.</h2>

<div class="row">
  <br>
  <img src="assets/img/burger.png" alt="yummy burger">
  <br>
</div>

<div class="row">   

  <h2>Want to add a burger?</h2>
  <br>
  <form class="create-update-form" action="burgers/create" method="POST">

    <div class="form-group">
      <label for="brg">Name Your Burger:</label>
      <input type="text" id="bg" name="name">
    </div>

    <button type="submit">Add Your Burger!</button>
  </form>

</div>  

<br>

<div class="row">

  <div class="col-lg-6">

    <h2>Devour one of these tasty burgers:</h2>
    <ul>
      {{#each burgers}}
        {{#unless this.devoured}}
          <li>
            {{this.burger_name}}
              <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="1" /> 
                <button type="devour">Devour it!</button>
              </form>

          </li>
        {{/unless}}
      {{/each}}
    </ul>

  </div>

  <div class="col-lg-6">

    <h2>I can't believe you ate all these burgers!!</h2>
    <ul>
      {{#each burgers}}
        {{#if this.devoured}}
          <li>
            {{this.burger_name}}

          </li>
        {{/if}}
      {{/each}}
    </ul>
  </div>  

</div>
</div>